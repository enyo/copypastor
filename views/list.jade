extends layout

block content

  table.table.table-striped
    thead
      tr
        th ID
        th Name
    tbody
      for textBlock in textBlocks
        tr
          td: a(href="/#{textBlock._id}")= textBlock._id
          td= textBlock.name
